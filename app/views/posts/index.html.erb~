<div class="container d-flex flex-column">
  <div class="fs-2 text-center">
    <div class="text-center fs-1" style="font-family: 'ZCOOL KuaiLe', cursive;">Welcome to <span style="color: #007693;">FacebookClone</span></div>
  </div>
<div class="text-center">
  Bienvenue <span style="background-color: #007693; font-family: 'ZCOOL KuaiLe', cursive;"><%= current_user.name %></span>
</div>
<div class="text-center">
<%= link_to "Logout", session_path(current_user.id), method: :DELETE, class: "btn btn-danger mt-2"%>
</div>
</div>
<div class="container">
<p class="text-center text-warning"><%= notice %> </p>
<div class="d-grid gap-2 col-6 mx-auto mt-5">
  <a class="btn btn-outline-success" href="<%= new_post_path%>">New tweet</a>
</div>
<p class="text-center fs-3 mt-5">Tout les tweet(s) <i class="bi bi-arrow-bar-down fs-1 text-warning"></i></p>
<% @posts.each do |post| %>

<article class="blog-post pb-5">
  <div class="d-flex ">
    <h5>Title: <%=post.title%> </h5>
    <% if session[:user_id] == post.user_id %>
      <a href="<%=edit_post_path(post.id)%>" class="btn btn-outline-info ms-5 btn-sm">Modifier</a>
      <a href="<%= post_path(post.id)%>" class="btn btn-outline-danger ms-5 btn-sm" data-confirm="Etes de vous sur ?" data-method="DELETE">Supprimer</a>
    <% end %>
    
  </div>
  <p class="ms-5 mt-5 text-justify"><%= post.content%></p>
  <p>
  <% if post.image.present? %>
    <%= image_tag post.image.url, :size => "900x900" %>
  <% end %>
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4"></div>
    <div class="col-md-4">
      {Auteur: <%= post.user.name if post.user_id.present? %>}
    </div>
  </div>
  </p>
</article>

<%end%>

</div>